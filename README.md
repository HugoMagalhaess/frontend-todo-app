# Todo app frontend (node, React, redux)

### Após clonar o projeto execute a instalação dos pacotes npm:

```npm install```

### Iniciar o projeto dev:

```npm run dev```

### Iniciar o projeto com pm2:

```npm run production```

